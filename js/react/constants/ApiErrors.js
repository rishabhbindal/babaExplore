export default ({
    bookingExists: 'Pending order for same user for similar request exists with order id =',
    orderStateExists: 'An existing Love for the given Property already exists, for this User',
    amountMismatch: 'amount_paid needs to be equal to amount_due or downpayment_amount'
});
