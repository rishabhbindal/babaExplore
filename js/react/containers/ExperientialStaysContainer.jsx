import React from 'react';

import SearchResultWrapperContainer from './SearchResultWrapperContainer.jsx';

const ExprientialStaysContainer = () => (
    <SearchResultWrapperContainer
      tags="ExperientialStays"
      redirectTo="/experiential-stays"
    />
);

export default ExprientialStaysContainer;
